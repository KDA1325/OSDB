using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemKeeper : MonoBehaviour
{


    //public GameObject broomPrefab;
    //public GameObject mopPrefab;

    //GameObject broomObj;
    //GameObject mopObj;

    // Start is called before the first frame update
    void Start()
    {
        //Vector3 pos = transform.position;
        //broomObj = Instantiate(broomPrefab, pos, Quaternion.identity);
        //broomObj.transform.SetParent(transform);
    }

    // Update is called once per frame
    void Update()
    {
        //float broomZ = -1;
        //PlayerController plmv = GetComponent<PlayerController>();
        //if(plmv.angleZ > 30 && plmv.angleZ < 150)
        //{
        //    broomZ = 1;
        //}
    }
}
